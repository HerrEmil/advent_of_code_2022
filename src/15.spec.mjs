import { strictEqual } from 'assert';
import { part1, part2 } from './15.mjs';

describe("Day 15 tests", () => {
  it("should ", () => {
    strictEqual(part1(1), 1)
  });
  it("should ", () => {
    strictEqual(part2(1), 1)
  });
});