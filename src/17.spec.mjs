import { strictEqual } from "assert";
import { parseData, part1, part2 } from "./17.mjs";

describe("Day 17 tests", () => {
  const data = parseData(``);

  it("should ", () => {
    strictEqual(part1(1), 1);
  });
  it("should ", () => {
    strictEqual(part2(1), 1);
  });
});
